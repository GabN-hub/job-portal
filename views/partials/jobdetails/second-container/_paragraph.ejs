<div class="second-container border border-green-500 rounded-md mt-8 w-full lg:col-span-2">
  <div class="header">
    <h3 class="p-6 bg-gray-500 rounded-md text-white"><%- title %></h3>
  </div>
  <div class="content p-6">
    <% content.forEach(paragraph => { %>
      <p><%= paragraph %></p>
    <% }); %>
  </div>
</div>
